# Goals Tracker

Goals Tracker is an AI-assisted personal planning app for turning specific goals into repeatable, realistic daily schedules.

## Development

Install dependencies:

```sh
npm install
```

Run the local app:

```sh
npm run dev
```

Verify the app shell:

```sh
npm test
npm run build
```
